export interface ProgramItem {
  id: string;
  tag: string;
  slug: string;
  title: string;
  description: string;
  weeks: string;
  rating: string;
  reviews: string;
}

export interface StepItem {
  number: string;
  title: string;
  description: string;
  color: string;
}